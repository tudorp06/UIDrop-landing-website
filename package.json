{
  "name": "uidrop-landing-website",
  "version": "1.0.0",
  "description": "UIDrop landing website with Vercel Web Analytics",
  "private": true,
  "scripts": {
    "build": "esbuild site/analytics.js --bundle --outfile=site/analytics.bundle.js --format=iife --minify"
  },
  "dependencies": {
    "@vercel/analytics": "^1.4.1"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
